# GraphQL Input Types
input TweetInput {
  text: String
  imageFile: Upload
}

# GraphQL Output Types
type Hashtag {
  text: String!
  indices: [Int!]!
}

type Tweet {
  userHandle: String!
  text: String!
  hashtags: [Hashtag]!
  imageURL: String
}
